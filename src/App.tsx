import { useState } from 'react'
import heroImg from './assets/hero.png'
import './App.css'
import DownloadButton from './components/atoms/download-button/DownloadButton'
import NavBar from './components/organisms/NavBar/NavBar'
import { PrimaryButton, SecondaryButton, LinkButton, Card, StarRating } from './components/atoms'
function App() {
  const [ratingAlpha, setRatingAlpha] = useState(2.5)
  const [ratingBeta, setRatingBeta] = useState(2.5)
  const [ratingGamma, setRatingGamma] = useState(2.5)

  return (
    <div className="app-root">
      <NavBar />

      <div className="app-container">
        <header className="hero-centered">
          <h1>Emotion Design System — Demo</h1>
          <p>Click to download a sample file generated by the component.</p>
        </header>

        <section className="feature-grid">
          <Card
              thumbnail={heroImg}
              title="Project Alpha"
              footer={<><PrimaryButton size="sm">Open</PrimaryButton><DownloadButton size="sm" label="Download" filename="alpha-sample.txt" /></>}
            >
            <div>A short description of Project Alpha. Uses design tokens for spacing and typography.</div>
            <div className="card__rating">
              <div className="rating-box">
                <StarRating value={ratingAlpha} onChange={(v) => setRatingAlpha(v)} size={18} />
              </div>
              <div className="rating-caption">Rating: {ratingAlpha}</div>
            </div>
          </Card>

          <Card
              thumbnail={heroImg}
              title="Project Beta"
              footer={<><SecondaryButton size="sm">Details</SecondaryButton><DownloadButton size="sm" label="Download" filename="beta-sample.txt" /></>}
            >
            <div>Project Beta has a different state and uses secondary actions.</div>
            <div className="card__rating">
              <div className="rating-box">
                <StarRating value={ratingBeta} onChange={(v) => setRatingBeta(v)} size={18} />
              </div>
              <div className="rating-caption">Rating: {ratingBeta}</div>
            </div>
          </Card>

          <Card
              thumbnail={heroImg}
              title="Project Gamma"
              footer={<><LinkButton size="sm">Learn more</LinkButton><DownloadButton size="sm" label="Download" filename="gamma-sample.txt" /></>}
            >
            <div>Project Gamma highlights the horizontal card layout.</div>
            <div className="card__rating">
              <div className="rating-box">
                <StarRating value={ratingGamma} onChange={(v) => setRatingGamma(v)} size={18} />
              </div>
              <div className="rating-caption">Rating: {ratingGamma}</div>
            </div>
          </Card>
        </section>

        <footer className="app-footer">
          <div className="footer-actions">
            <PrimaryButton>Primary</PrimaryButton>
            <SecondaryButton>Secondary</SecondaryButton>
            <LinkButton>Link style</LinkButton>
          </div>

          <div className="footer-actions">
            <DownloadButton label="Download sample" filename="sample.txt" />
          </div>
        </footer>
      </div>
    </div>
  )
}

export default App
